import { FormatTransmisionPipe } from './format-transmision.pipe';

describe('FormatTransmisionPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTransmisionPipe();
    expect(pipe).toBeTruthy();
  });
});
